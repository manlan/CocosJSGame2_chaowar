var GC = GC || {};

GC.gameVersion = "1.0.0";

GC.winSize = cc.size(1136, 640);

GC.gameSize = cc.size(960, 640);

GC.MainContentSize = cc.size(960, 640  - 99);

GC.h = GC.winSize.height;

GC.w = GC.winSize.width;

GC.w_2 = GC.winSize.width / 2 ;

GC.h_2 = GC.winSize.height / 2;

GC.CCSizeZero = cc.rect(0, 0, 0, 0);

GC.SOUND_ON = true;